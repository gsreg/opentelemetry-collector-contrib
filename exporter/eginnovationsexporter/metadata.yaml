type: eginnovations

status:
  class: exporter
  stability:
    development: [traces]
  codeowners:
    active: [gsreg]

tests:
  config:
    endpoint: "eginnovations.com"
  expect_consumer_error: true
  goleak:
    skip: true